# Kalshi Roku Demo

Password-protected demo page. The content is AES-256 encrypted inside `index.html` and decrypts in the browser — no password, no content.

Open the published page, enter the password, then click once and navigate with the arrow keys (D-pad), Enter (OK), and Esc/Backspace (Back).
